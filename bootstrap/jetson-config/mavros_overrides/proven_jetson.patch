diff --git a/mavros/launch/px4_config.yaml b/mavros/launch/px4_config.yaml
index e0cdca70..31e705cd 100644
--- a/mavros/launch/px4_config.yaml
+++ b/mavros/launch/px4_config.yaml
@@ -4,6 +4,10 @@
   ros__parameters:
     startup_px4_usb_quirk: false
 
+    # --- CORE MAVROS ROUTER FRAMES ---
+    base_link_frame_id: "drone_link"   # Forces MAVROS to build drone_link_frd
+    map_frame_id: "ground"             # Forces MAVROS to build ground_ned
+    odom_frame_id: "odom_tilt"         # Forces MAVROS to build odom_tilt_ned
 # --- system plugins ---
 
 # sys_status
@@ -48,20 +52,20 @@
 # global_position
 /**/global_position:
   ros__parameters:
-    frame_id: "map"               # origin frame
-    child_frame_id: "base_link"   # body-fixed frame
+    frame_id: "ground"               # origin frame
+    child_frame_id: "drone_link"   # body-fixed frame
     rot_covariance: 99999.0       # covariance for attitude?
     gps_uere: 1.0                 # User Equivalent Range Error (UERE) of GPS sensor (m)
     use_relative_alt: true        # use relative altitude for local coordinates
     tf.send: false                # send TF?
-    tf.frame_id: "map"            # TF frame_id
+    tf.frame_id: "ground"            # TF frame_id
     tf.global_frame_id: "earth"   # TF earth frame_id
-    tf.child_frame_id: "base_link"  # TF child_frame_id
+    tf.child_frame_id: "drone_link"  # TF child_frame_id
 
 # imu_pub
 /**/imu:
   ros__parameters:
-    frame_id: "base_link"
+    frame_id: "drone_link"
     # need find actual values
     linear_acceleration_stdev: 0.0003
     angular_velocity_stdev: 0.0003490659  # 0.02 degrees
@@ -71,10 +75,10 @@
 # local_position
 /**/local_position:
   ros__parameters:
-    frame_id: "map"
+    frame_id: "ground"
     tf.send: false
-    tf.frame_id: "map"
-    tf.child_frame_id: "base_link"
+    tf.frame_id: "ground"
+    tf.child_frame_id: "drone_link"
     tf.send_fcu: false
 
 # param
@@ -94,7 +98,7 @@
     reverse_thrust: false       # allow reversed thrust
     use_quaternion: false       # enable PoseStamped topic subscriber
     tf.listen: false            # enable tf listener (disable topic subscribers)
-    tf.frame_id: "map"
+    tf.frame_id: "ground"
     tf.child_frame_id: "target_attitude"
     tf.rate_limit: 50.0
 
@@ -109,7 +113,7 @@
 /**/setpoint_position:
   ros__parameters:
     tf.listen: false            # enable tf listener (disable topic subscribers)
-    tf.frame_id: "map"
+    tf.frame_id: "ground"
     tf.child_frame_id: "target_position"
     tf.rate_limit: 50.0
     mav_frame: LOCAL_NED
@@ -118,7 +122,7 @@
 /**/guided_target:
   ros__parameters:
     tf.listen: false           # enable tf listener (disable topic subscribers)
-    tf.frame_id: "map"
+    tf.frame_id: "ground"
     tf.child_frame_id: "target_position"
     tf.rate_limit: 50.0
 
@@ -190,20 +194,20 @@
 /**/fake_gps:
   ros__parameters:
     # select data source
-    use_mocap: true         # ~mocap/pose
-    mocap_transform: true   # ~mocap/tf instead of pose
-    use_vision: false       # ~vision (pose)
-    # origin (default: Zürich)
-    geo_origin.lat: 47.3667       # latitude [degrees]
-    geo_origin.lon: 8.5500        # longitude [degrees]
-    geo_origin.alt: 408.0         # altitude (height over the WGS-84 ellipsoid) [meters]
+    use_mocap: false        # ~mocap/pose
+    mocap_transform: false  # ~mocap/tf instead of pose
+    use_vision: true        # ~vision (pose)
+    # origin (Pittsburgh)
+    geo_origin.lat: 40.41367045298802
+    geo_origin.lon: -79.94633160928028
+    geo_origin.alt: 300.0
     eph: 2.0
     epv: 2.0
-    satellites_visible: 5   # virtual number of visible satellites
+    satellites_visible: 10  # virtual number of visible satellites
     fix_type: 3             # type of GPS fix (default: 3D)
     tf.listen: false
     tf.send: false           # send TF?
-    tf.frame_id: "map"       # TF frame_id
+    tf.frame_id: "ground"       # TF frame_id
     tf.child_frame_id: "fix" # TF child_frame_id
     tf.rate_limit: 10.0      # TF rate
     gps_rate: 5.0           # GPS data publishing rate
@@ -244,9 +248,9 @@
 # odom
 /**/odometry:
   ros__parameters:
-    fcu.odom_parent_id_des: "odom"        # desired parent frame rotation of the FCU's odometry
-    fcu.odom_child_id_des: "base_link"    # desired child frame rotation of the FCU's odometry
-    fcu.map_id_des: "map"
+    fcu.odom_parent_id_des: "odom_tilt"        # desired parent frame rotation of the FCU's odometry
+    fcu.odom_child_id_des: "drone_link"    # desired child frame rotation of the FCU's odometry
+    fcu.map_id_des: "ground"
 
 # px4flow
 /**/px4flow:
@@ -260,7 +264,7 @@
 /**/vision_pose:
   ros__parameters:
     tf.listen: false           # enable tf listener (disable topic subscribers)
-    tf.frame_id: "odom"
+    tf.frame_id: "odom_tilt"
     tf.child_frame_id: "vision_estimate"
     tf.rate_limit: 10.0
 
@@ -273,7 +277,7 @@
 # vibration
 /**/vibration:
   ros__parameters:
-    frame_id: "base_link"
+    frame_id: "drone_link"
 
 # wheel_odometry
 /**/wheel_odometry:
@@ -284,14 +288,14 @@
     wheel1: {x: 0.0, y:  0.15, radius: 0.05}  # x-, y-offset (m,NED) and radius (m)
     send_raw: true                # send wheel's RPM and cumulative distance (~/wheel_odometry/rpm, ~/wheel_odometry/distance)
     send_twist: false             # send geometry_msgs/TwistWithCovarianceStamped instead of nav_msgs/Odometry
-    frame_id: "odom"              # origin frame
-    child_frame_id: "base_link"   # body-fixed frame
+    frame_id: "odom_tilt"              # origin frame
+    child_frame_id: "drone_link"   # body-fixed frame
     vel_error: 0.1                # wheel velocity measurement error 1-std (m/s)
     tf.send: false
-    tf.frame_id: "odom"
-    tf.child_frame_id: "base_link"
+    tf.frame_id: "odom_tilt"
+    tf.child_frame_id: "drone_link"
 
 # camera
 /**/camera:
   ros__parameters:
-    frame_id: "base_link"
+    frame_id: "camera_link"
diff --git a/mavros_extras/src/plugins/fake_gps.cpp b/mavros_extras/src/plugins/fake_gps.cpp
index 217a7454..ea8164ad 100644
--- a/mavros_extras/src/plugins/fake_gps.cpp
+++ b/mavros_extras/src/plugins/fake_gps.cpp
@@ -87,7 +87,7 @@ public:
   {
     enable_node_watch_parameters();
 
-    last_pos_time = rclcpp::Time(0.0);
+    last_pos_time = rclcpp::Time(0, 0, RCL_SYSTEM_TIME);
 
     // general params
     node_declare_and_watch_parameter(
